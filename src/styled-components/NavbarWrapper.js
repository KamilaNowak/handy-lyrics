import styledComponents from 'styled-components'

export const NavbarWrapper = styledComponents.nav`
background: var(--lightPurple);
.nav-link{
    color: var(--primaryWhite)!important;
    font-size: 1.5rem;
    text-transform: capitalize !important;
}
`;